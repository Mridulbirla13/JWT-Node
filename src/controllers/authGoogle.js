// // controllers/authGoogle.js
// const passport = require('../services/authGoogle');

// const googleAuth = passport.authenticate('google', { scope: ['email','profile'] });

// const googleCallback = passport.authenticate('google', { failureRedirect: '/login', successRedirect: '/dashboard' });

// module.exports = { googleAuth, googleCallback };
